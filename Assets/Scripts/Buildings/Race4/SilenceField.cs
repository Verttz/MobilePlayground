using UnityEngine;

// Silence Field - Prevents enemy drops; grants temporary attack buffs when drops would have occurred
public class SilenceField : Building
{
    // Support building that prevents drops for buffs
}
